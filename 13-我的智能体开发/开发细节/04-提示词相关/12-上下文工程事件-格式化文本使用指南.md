# 上下文工程事件 - 格式化文本使用指南

## 📋 目录

1. [概述](#概述)
2. [Protobuf 定义](#protobuf-定义)
3. [客户端接收示例](#客户端接收示例)
4. [使用场景](#使用场景)
5. [与写入文件的一致性](#与写入文件的一致性)
6. [常见问题](#常见问题)

---

## 概述

从 2026-01-05 版本开始，`ContextEngineeringEvent` 新增了 `formatted_text` 字段，该字段包含与写入 `context_engineering_events.txt` 文件**完全一致**的格式化文本。

### 为什么需要 formatted_text？

**之前的问题**：
- 客户端收到的是结构化的 protobuf 数据（`SuccessfulStepInfo`、`FailedStepInfo` 等）
- 需要客户端自己实现格式化逻辑才能展示人类可读的文本
- 服务端写入文件的格式和客户端展示的格式不一致

**现在的优势**：
- ✅ 服务端生成一次，客户端直接使用
- ✅ 格式完全一致（文件和客户端接收到的完全相同）
- ✅ 人类可读，易于展示、日志记录、知识库存储
- ✅ 向后兼容（仍然保留结构化数据字段）

---

## Protobuf 定义

### 完整定义

```protobuf
message ContextEngineeringEvent {
    // 任务描述
    string task_description = 1;

    // 成功步骤列表（结构化数据）
    repeated SuccessfulStepInfo successful_steps = 2;

    // 失败步骤列表（结构化数据）
    repeated FailedStepInfo failed_steps = 3;

    // 【新增】格式化的人类可读文本（与写入文件的格式一致）
    string formatted_text = 4;
}
```

### 字段说明

| 字段 | 类型 | 说明 | 用途 |
|------|------|------|------|
| `task_description` | string | 任务描述 | 快速了解任务内容 |
| `successful_steps` | repeated | 成功步骤列表（结构化） | 需要程序化处理时使用 |
| `failed_steps` | repeated | 失败步骤列表（结构化） | 需要程序化处理时使用 |
| `formatted_text` | **string** | **格式化文本（人类可读）** | **直接展示、日志、知识库** |

---

## 客户端接收示例

### 示例 1: 基础接收（Rust）

```rust
use task_orchestration_service::grpc::orchestrator::{
    task_orchestrator_service_client::TaskOrchestratorServiceClient,
    StreamTaskExecutionRequest, EventType,
    task_execution_event::EventData,
};
use tokio_stream::StreamExt;

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    // 1. 连接到服务
    let channel = tonic::transport::Channel::from_static("http://127.0.0.1:8084")
        .connect()
        .await?;
    let mut client = TaskOrchestratorServiceClient::new(channel);

    // 2. 提交任务（省略...）
    let task_id = "...";

    // 3. 监听事件流
    let stream_request = StreamTaskExecutionRequest {
        task_id: task_id.to_string()
    };
    let response = client.stream_task_execution(stream_request).await?;
    let mut stream = response.into_inner();

    while let Some(event_result) = stream.next().await {
        let event = event_result?;
        let event_type = EventType::try_from(event.event_type)?;

        if event_type == EventType::ContextEngineering {
            if let Some(EventData::ContextEngineering(context_event)) = event.event_data {
                // ====== 【关键】直接使用格式化文本 ======
                println!("收到上下文工程事件:");
                println!("{}", context_event.formatted_text);

                // 统计信息
                println!("成功步骤: {}", context_event.successful_steps.len());
                println!("失败步骤: {}", context_event.failed_steps.len());
            }
        }
    }

    Ok(())
}
```

### 示例 2: 保存到文件

```rust
// 接收到事件后，直接保存到文件
if !context_event.formatted_text.is_empty() {
    tokio::fs::write(
        "context_engineering_events_client.txt",
        &context_event.formatted_text
    ).await?;

    println!("✅ 已保存到文件");
}
```

### 示例 3: 发送到日志服务（Elasticsearch）

```rust
use serde_json::json;

// 构建 Elasticsearch 文档
let doc = json!({
    "timestamp": chrono::Utc::now().to_rfc3339(),
    "task_description": context_event.task_description,
    "formatted_content": context_event.formatted_text,
    "successful_steps_count": context_event.successful_steps.len(),
    "failed_steps_count": context_event.failed_steps.len(),
});

// 发送到 Elasticsearch
let client = reqwest::Client::new();
client.post("http://localhost:9200/context-engineering/_doc")
    .json(&doc)
    .send()
    .await?;
```

### 示例 4: 发送到消息队列（Kafka）

```rust
use rdkafka::producer::{FutureProducer, FutureRecord};

// 构建 Kafka 消息
let producer: FutureProducer = /* ... */;
let payload = context_event.formatted_text.as_bytes();

producer.send(
    FutureRecord::to("context-engineering-events")
        .key("task")
        .payload(payload),
    std::time::Duration::from_secs(5)
).await?;
```

### 示例 5: 存储到知识库

```rust
// 提取关键信息存储到知识库
struct KnowledgeEntry {
    task_type: String,
    formatted_content: String,
    successful_steps: Vec<String>,
    failed_steps: Vec<String>,
    created_at: String,
}

let entry = KnowledgeEntry {
    task_type: context_event.task_description.clone(),
    formatted_content: context_event.formatted_text.clone(),
    successful_steps: context_event.successful_steps
        .iter()
        .map(|s| s.step_name.clone())
        .collect(),
    failed_steps: context_event.failed_steps
        .iter()
        .map(|s| s.step_name.clone())
        .collect(),
    created_at: chrono::Utc::now().to_rfc3339(),
};

// 存储到数据库或向量数据库（如 Milvus）
knowledge_db.insert(entry).await?;
```

---

## 使用场景

### 场景 1: 用户界面展示

**需求**：在 Web 界面或桌面应用中展示任务执行结果

**方案**：
```rust
// 直接将 formatted_text 展示到界面
let formatted_html = format!(
    "<pre style='background: #f5f5f5; padding: 10px;'>{}</pre>",
    html_escape::encode_text(&context_event.formatted_text)
);
ui.show_html(&formatted_html);
```

**优势**：
- ✅ 无需客户端格式化逻辑
- ✅ 格式统一、美观
- ✅ 减少客户端开发工作量

---

### 场景 2: 日志审计

**需求**：记录所有任务执行的详细日志，用于审计和问题追溯

**方案**：
```rust
// 写入日志文件
let log_file = format!("logs/task_{}.log", task_id);
tokio::fs::write(log_file, &context_event.formatted_text).await?;

// 或发送到集中式日志系统
logger.info(
    "Task execution completed",
    json!({
        "task_id": task_id,
        "content": context_event.formatted_text,
    })
);
```

---

### 场景 3: 上下文工程/知识提取

**需求**：将任务执行经验存储到知识库，用于未来相似任务的参考

**方案**：
```rust
// 1. 解析格式化文本，提取关键信息
let knowledge = parse_context_engineering_text(&context_event.formatted_text);

// 2. 向量化并存储到向量数据库
let embedding = embedding_model.encode(&knowledge.content);
vector_db.insert(knowledge, embedding).await?;

// 3. 未来查询
let similar_tasks = vector_db.search(&query_embedding, top_k=5).await?;
```

---

### 场景 4: 实时监控

**需求**：实时监控任务执行情况，及时发现问题

**方案**：
```rust
// 提取失败步骤数量
let failed_count = context_event.failed_steps.len();

if failed_count > 0 {
    // 发送告警
    alerting_service.send_alert(
        AlertLevel::Warning,
        format!("Task {} has {} failed steps", task_id, failed_count),
        context_event.formatted_text,
    ).await?;
}

// 更新监控指标
metrics.task_completed_total.inc();
metrics.failed_steps_total.inc_by(failed_count as u64);
```

---

### 场景 5: 多语言客户端集成

**需求**：Python、Go、Java 等客户端也需要接收上下文工程事件

**Python 示例**：
```python
import grpc
from orchestrator_pb2 import StreamTaskExecutionRequest
from orchestrator_pb2_grpc import TaskOrchestratorServiceStub

# 连接服务
channel = grpc.insecure_channel('localhost:8084')
stub = TaskOrchestratorServiceStub(channel)

# 监听事件流
for event in stub.StreamTaskExecution(StreamTaskExecutionRequest(task_id=task_id)):
    if event.event_type == EventType.CONTEXT_ENGINEERING:
        context_event = event.context_engineering

        # 直接使用格式化文本
        print("收到上下文工程事件:")
        print(context_event.formatted_text)

        # 保存到文件
        with open('context_engineering_events.txt', 'w', encoding='utf-8') as f:
            f.write(context_event.formatted_text)
```

**Go 示例**：
```go
import (
    "context"
    "fmt"
    "io/ioutil"
    pb "path/to/orchestrator"
)

// 监听事件流
stream, _ := client.StreamTaskExecution(context.Background(), &pb.StreamTaskExecutionRequest{
    TaskId: taskId,
})

for {
    event, err := stream.Recv()
    if err != nil {
        break
    }

    if event.EventType == pb.EventType_CONTEXT_ENGINEERING {
        contextEvent := event.GetContextEngineering()

        // 直接使用格式化文本
        fmt.Println("收到上下文工程事件:")
        fmt.Println(contextEvent.FormattedText)

        // 保存到文件
        ioutil.WriteFile("context_engineering_events.txt",
            []byte(contextEvent.FormattedText), 0644)
    }
}
```

---

## 与写入文件的一致性

### 数据来源

**服务端实现**（`src/core/orchestrator.rs`）:

```rust
/// 构建最终的上下文工程事件
pub fn build(&self) -> ContextEngineeringEvent {
    // ... 构建结构化数据 ...

    // 生成格式化的人类可读文本（与 format_to_file 方法保持一致）
    let formatted_text = self.format_to_file();

    ContextEngineeringEvent {
        task_description: self.task_description.clone(),
        successful_steps,
        failed_steps,
        formatted_text,  // 与写入文件的内容完全一致
    }
}
```

### 格式示例

```text
================================================================================
任务描述: 意图: 工具管理, 用户输入: 创建一个简单的计算器工具，支持基本四则运算

成功步骤列表 (共 1 个):
--------------------------------------------------------------------------------


步骤 #1: 生成计算器工具设计方案
  步骤ID: step_1
  步骤描述: 包含工具名称、功能描述、输入参数、输出格式、实现思路的方案文档
  工具ID: genesis_document_generate
  依赖步骤: (无依赖)

  工具参数:
    - user_requirements: "创建一个简单的计算器工具，支持基本四则运算"

  工具输出:
    {"plans":[{"file_name":"architecture_qwen.md", ...}]}


失败步骤列表 (共 0 个):
--------------------------------------------------------------------------------


================================================================================
```

### 验证一致性

**测试代码**：
```rust
// 1. 读取服务端写入的文件
let file_content = tokio::fs::read_to_string("context_engineering_events.txt").await?;

// 2. 接收客户端事件
let client_content = context_event.formatted_text;

// 3. 验证一致性
assert_eq!(file_content, client_content, "文件和客户端接收的内容应完全一致");
```

---

## 常见问题

### Q1: 我可以只使用 formatted_text，忽略结构化数据吗？

**答**：可以！`formatted_text` 已经包含了所有信息。但如果你需要程序化处理（如统计分析、数据库查询），结构化数据更方便。

**建议**：
- 展示给用户：使用 `formatted_text`
- 程序化处理：使用 `successful_steps` 和 `failed_steps`

---

### Q2: formatted_text 字段为空怎么办？

**答**：这种情况只会在以下情况发生：
1. 服务端版本过旧（2026-01-05 之前）
2. 配置中禁用了上下文工程事件

**处理方案**：
```rust
if context_event.formatted_text.is_empty() {
    // 回退到结构化数据展示
    println!("未收到格式化文本，使用结构化数据:");
    for step in &context_event.successful_steps {
        println!("  - {}: {}", step.step_name, step.step_description);
    }
} else {
    // 使用格式化文本
    println!("{}", context_event.formatted_text);
}
```

---

### Q3: formatted_text 的大小限制是多少？

**答**：
- gRPC 消息默认限制：4MB
- 客户端可以调整：`.max_decoding_message_size(64 * 1024 * 1024)` （64MB）
- 实际大小取决于步骤数量和输出内容

**建议**：
- 如果步骤输出过长，会自动截断（前 10 行）
- 如果仍然过大，可以只使用结构化数据

---

### Q4: 如何在非 Rust 语言中使用？

**答**：所有支持 protobuf 和 gRPC 的语言都可以使用：

1. **生成客户端代码**：
```bash
# Python
python -m grpc_tools.protoc -I proto --python_out=. --grpc_python_out=. proto/*.proto

# Go
protoc --go_out=. --go-grpc_out=. proto/*.proto

# Java
protoc --java_out=src/main/java --grpc-java_out=src/main/java proto/*.proto
```

2. **访问字段**：
```python
# Python
formatted_text = context_event.formatted_text

# Go
formattedText := contextEvent.GetFormattedText()

# Java
String formattedText = contextEvent.getFormattedText();
```

---

### Q5: 我需要同时保存到文件和发送到客户端吗？

**答**：取决于你的需求：

| 场景 | 文件保存 | 客户端发送 | 建议 |
|------|---------|-----------|------|
| 单机部署 | ✅ | ❌ | 只需文件 |
| 分布式部署 | ✅ | ✅ | 文件用于服务端审计，客户端用于实时展示 |
| 纯客户端 | ❌ | ✅ | 客户端自己决定是否保存 |
| 云原生 | ❌ | ✅ | 发送到集中式存储（S3、OSS） |

---

## 运行示例

### 完整示例代码

位置：`examples/context_engineering_formatted_text.rs`

### 运行步骤

1. **启动服务**：
```bash
cargo run --release
```

2. **运行示例**：
```bash
cargo run --example context_engineering_formatted_text
```

3. **查看输出**：
- 控制台：实时输出格式化文本
- 文件：`context_engineering_events_client.txt`

---

## 总结

| 特性 | 说明 |
|------|------|
| **新字段** | `formatted_text`（人类可读文本） |
| **格式一致性** | 与服务端写入文件完全一致 |
| **向后兼容** | 保留结构化数据字段 |
| **使用场景** | 界面展示、日志记录、知识库、监控 |
| **多语言支持** | 通过 protobuf/gRPC 支持所有语言 |
| **最佳实践** | 展示用 `formatted_text`，程序处理用结构化数据 |

**核心优势**：
- ✅ **简单**：客户端无需格式化逻辑
- ✅ **一致**：服务端生成一次，到处使用
- ✅ **可读**：人类友好的格式
- ✅ **灵活**：支持多种存储和传输方式
