//! 用户友好消息模板模块
//!
//! 包含用于生成用户友好的进度消息和状态更新的模板
//!
//! # 功能特性
//!
//! - 输出风格转换（技术性 → 用户友好）
//! - 各阶段进度消息模板
//! - 消息润色功能
//! - 支持多样化表达
//! - 支持场景感知的消息生成

use super::scene_guidance::SceneManager;

/// 输出风格模板 - 用于��技术性内容转换为用户友好的叙事风格
pub const OUTPUT_STYLE_TEMPLATE: &str = r#"
请将以下内容转换为用户友好的叙事风格：

{content}

要求：
1. **完整保留所有信息**：
   - 格式标记（如「」包裹的名称）必须保留
   - 所有数字数据（评估得分、统计、百分比、ID、时间等）一个不能少
   - 所有关键字段（如report_id、title、summary、highlights、file_url等）都要体现
   - 原文有几个信息点，转换后必须也有几个信息点

2. **格式要求 - 丰富的Markdown格式**：
   - **主标题**：用 ## 开头，emoji + 简短状态，不超过20字
   - **章节标题**：用 ## 开头（二级标题），简短标题如"核心成果"、"详细信息"等
   - **章节数量**：信息多时2-4个，信息少时1-2个
   - **内容呈现方式**：**优先使用表格，其次列表，禁止使用段落叙述**
   - **分隔线**：章节之间可以用 `---` 分隔线增强视觉效果
   - **空行分隔**：主标题后空行，表格/列表前后要有空行
   - **禁止输出**：禁止生成"正在准备中，请稍候"、"很快就能开始啦"等提示性文字

3. **表格 vs 列表 - 智能选择**：

   **✅ 使用表格的场景**：
   - 结构化的键值对数据（如配置信息、技术参数）
   - 统计数据、指标数据（如采集点数、覆盖率、耗时等）
   - 文件属性信息（文件名、格式、大小、路径）
   - 对比性数据（多个维度的对比）
   - 数据量 ≥ 3个字段时

   **示例**（适合用表格）：
   ```
   | 指标 | 数值 |
   |------|------|
   | 采集点数 | 456个 |
   | 覆盖范围 | 全楼18层 |
   | 数据完整性 | 98% |
   | 采集耗时 | 2.3秒 |
   ```

   **✅ 使用列表的场景**：
   - 要点总结、关键信息罗列
   - 步骤说明、操作流程
   - 成果描述（带有解释说明的内容）
   - 下一步计划、待办事项
   - 错误描述、处理方案
   - 内容需要详细描述时（不是简单的键值对）

   **示例**（适合用列表）：
   ```
   - 建筑模型: 18层写字楼，包含1,250个房间
   - 设备节点: 125个设备，建立了340条连接关系
   - 下载链接: [building.ifc](/path/to/file)
   ```

4. **混合使用原则**：
   - **一个章节内统一格式**：同一个 ## 章节下只用一种格式（要么表格，要么列表）
   - **不同章节可以不同**：第一个章节用表格，第二个章节用列表
   - **根据内容性质决定**：数据型用表格，描述型用列表
   - 禁止使用"刚刚"、"已经"、"正在准备中"、"请稍候"等叙述性连接词

5. **其他Markdown元素**：
   - **分割线**：章节之间用 `---` 分隔，增强层次感
   - **超链接**：如有文件路径或URL，使用 `[文件名](路径)` 格式
   - **粗体强调**：重要字段用 `**粗体**` 标记
   - **代码标记**：技术术语可用 `` `code` `` 包裹

6. **语气要求**：
   - 内容要简洁专业，不要过于口语化
   - 避免"刚刚"、"已经"等时间性词汇
   - 技术术语可以保留，但加上简要说明

7. **信息处理**：
   - 如果原文说某字段为空，写成"暂未填充"、"待补充"等
   - 根据内容性质选择表格或列表
   - 所有ID、数字、时间都要**完整展示**，绝对禁止省略或缩写
   - **禁止使用省略号**：如 `2d4eff6e...` 是错误的，必须完整显示为 `2d4eff6e-f4ca-4dc6-b776-1d838b8cefb5`
   - **禁止截断任何字段**：UUID、路径、URL等必须完整显示

8. **格式多样化要求**：
⚠️ **重要**: 智能选择格式，表格和列表混合使用！

**格式选择决策树**：
1. 看数据性质：是键值对? → 用表格
2. 看内容复杂度：需要详细描述? → 用列表
3. 看数据量：≥3个字段? → 用表格
4. 看表达需求：需要对比? → 用表格
5. 看内容类型：步骤/成果/方案? → 用列表

**章节标题要灵活变化**（根据内容选择合适的标题）：
- 核心成果 / 执行结果 / 完成情况 / 主要内容
- 详细信息 / 具体数据 / 关键指标 / 技术细节
- 当前状态 / 执行状态 / 任务状态 / 进度信息
- 文件信息 / 输出文件 / 生成内容 / 数据报表
- 下一步 / 后续步骤 / 待执行 / 即将进行
- 问题描述 / 错误详情 / 异常信息（失败时）
- 数据统计 / 统计概览 / 关键数据

**智能混合示例1**（数据用表格，步骤用列表）：
## ✅ 数据采集完成(2/5)

## 采集概况

| 指标 | 数值 |
|------|------|
| 采集点数 | 456个 |
| 覆盖范围 | 全楼18层 |
| 数据完整性 | 98% |
| 采集耗时 | 2.3秒 |

---

## 下一步
- 即将执行「数据清洗与处理」步骤

**智能混合示例2**（参数用表格，成果用列表）：
## ✅ 建筑建模完成(1/3)

## 模型参数

| 项目 | 详情 |
|------|------|
| 建筑类型 | 18层商业写字楼 |
| 房间总数 | 1,250个 |
| 模型精度 | `LOD300` (高精度) |
| 建筑面积 | 45,000 m² |

---

## 生成成果
- 成功创建了完整的建筑几何模型
- 所有房间边界清晰，空间关系准确
- 模型文件: [building_001.ifc](/models/building_001.ifc) - IFC格式，156MB

**智能混合示例3**（错误用表格，方案用列表）：
## ❌ 设备连接失败(2/4)

## 错误详情

| 字段 | 内容 |
|------|------|
| 错误类型 | 函数未定义 |
| 函数名称 | `create_connection` |
| 影响范围 | 设备拓扑无法建立 |

---

## 处理方案
- 系统正在分析函数缺失原因
- 准备使用备用连接方法重试
- 预计3分钟内完成修复

9. **长度控制**：
   - 不超过 {max_length} 字
   - 优先保证信息完整
   - 表格和列表都很精炼

10. **绝对禁止**：
   - 禁止删减任何数据或字段
   - 禁止添加原文没有的信息
   - 禁止使用段落叙述，必须用表格或列表
   - 禁止总用同一种格式，要灵活变化
   - 禁止生成"正在准备中"、"请稍候"、"很快就能开始"等提示性文字
   - **禁止省略或截断任何数据**：UUID、ID、路径、URL等必须完整显示，不得使用 `...` 省略
   - **禁止简化数据**：如 `2d4eff6e...` 必须写完整的 `2d4eff6e-f4ca-4dc6-b776-1d838b8cefb5`

直接输出转换后的内容，不要有其他解释。
"#;

/// 工具筛选阶段消息模板
pub const TOOL_SELECTION_MESSAGE_TEMPLATE: &str = r#"
任务：用户刚提交了一个任务，系统正在从工具库中筛选最适合的工具。

用户任务描述：{task_description}

筛选到的工具数量：{tool_count}

生成内容要求：
- **使用Markdown标题**：用 ## 开头
- 确认收到任务，**原封不动地引用用户的任务描述**（用「」包裹）
- 简要说明正在筛选工具，可以提及工具数量
- 语气自然、专业、简洁

示例（假设用户输入是"为18层写字楼建立数字孪生模型"，工具数量是52）：
第一行：## 🔍 正在筛选工具
空行
内容：收到您的「为18层写字楼建立数字孪生模型」任务，正在从52个工具中为您筛选最佳组合。

或者：
第一行：## 🛠️ 任务已接收
空行
内容：「为18层写字楼建立数字孪生模型」任务已启动，52个工具已就位、正在智能匹配。

注意：
1. 必须用 ## 开头作为标题
2. 标题后空行
3. 任务描述必须使用用户输入的原文，不能修改

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【场景指导】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{scene_specific_guidance}
"#;

/// 任务规划阶段消息模板
pub const TASK_PLANNING_MESSAGE_TEMPLATE: &str = r#"
任务：工具已经选好，现在要为用户制定详细的执行计划。

用户任务描述：{task_description}

已选工具数量：{tool_count}

计划步骤数：{step_count}

生成内容要求：
- **使用Markdown标题**：用 ## 开头
- 说明工具已准备就绪，正在制定执行方案
- 可以提及工具数量或步骤数
- 语气自然、简洁

示例（假设已选16个工具，共6个步骤）：
第一行：## 📋 正在制定执行方案
空行
内容：16个工具已配齐，正在为您规划6个执行步骤。

或者：
第一行：## 🎯 准备执行
空行
内容：工具已就位，开始制定分步执行计划。

注意：
1. 必须用 ## 开头作为标题
2. 标题后空行
3. 保持简洁
"#;

/// 重新规划阶段消息模板
pub const REPLANNING_MESSAGE_TEMPLATE: &str = r#"
任务：执行过程中遇到问题，需要重新规划任务。

之前执行摘要：{execution_summary}

遇到的核心问题：{issue}

已完成的步骤数：{completed_steps_count}

失败的步骤名称：{failed_step_name}

生成内容要求：
1. **使用Markdown标题**：用 ## 开头
2. **第一句承认问题**：温和地承认遇到了问题（可引用具体步骤名「」）
3. **第二句肯定成果**：肯定已完成的步骤成果（如"前X个步骤已成功完成"）
4. **第三句行动方案**：说明正在重新规划，传达找到更好方案的信心
5. **语气**：积极、不回避问题、给用户信心
6. **总字数**：50-80字

示例1（有已完成步骤）：
## 🔄 正在优化执行方案

「设备拓扑构建」步骤遇到了函数缺失问题。前2个步骤（几何建模、数据采集）已成功完成，正在为您重新规划后续步骤，换个更稳妥的方案继续推进。

示例2（首个步骤失败）：
## 🔄 调整执行策略

「BMS数据采集」步骤遇到连接超时问题，正在为您分析原因并重新制定采集方案，确保数据获取的稳定性。

示例3（多个步骤失败）：
## 🔄 重新规划任务路径

执行过程中遇到了工具兼容性问题，已完成的「建筑建模」步骤成果保留，正在为您设计新的执行路线，避开问题区域。

注意：
- 必须用 ## 开头作为Markdown标题
- 标题后空行再接正文
- 如果有已完成步骤，必须提及并肯定
- 不要使用"失败"等负面词汇，用"遇到问题"、"需要调整"等中性表达
"#;

/// 继续规划阶段消息模板
pub const CONTINUE_PLANNING_MESSAGE_TEMPLATE: &str = r#"
任务：用户希望继续之前的任务执行，需要基于历史执行情况继续规划后续步骤。

历史执行总结：{execution_summary}

当前可用工具数量：{tool_count}

生成内容要求：
- **第一句**：简要总结之前完成的内容（如"基于之前X个步骤的执行情况"或"继续刚才的任务"）
- **第二句**：说明接下来要做什么（如"正在规划后续步骤"或"继续为您执行剩余任务"）
- 体现"继续"的连贯性，而非"重新开始"
- 语气自然、积极、简洁（40字以内）

示例：
"好的！基于之前3个步骤的执行情况，正在为您规划接下来的任务步骤..."
"继续刚才的任务！已完成前期准备，现在为您制定后续执行方案..."
"明白！根据之前的执行进度，正在继续规划剩余步骤..."
"收到！基于当前进展，为您继续制定详细执行计划..."

注意：必须体现"继续"的概念，让用户感知到这是基于之前工作的延续，而不是从头开始。
"#;

/// 继续规划工具筛选完成消息模板
pub const CONTINUE_PLANNING_TOOL_SELECTION_COMPLETED_TEMPLATE: &str = r#"
任务：用户继续之前的任务，系统已完成工具重新筛选。

之前执行情况摘要：{execution_summary}

之前使用的工具数量：{previous_tool_count}

当前筛选到的工具数量：{current_tool_count}

工具变化类型：{change_type}
- 如果 current > previous: "新增"
- 如果 current < previous: "优化精简"
- 如果 current = previous: "调整"

保留的工具：{kept_tools}

替换的工具：{replaced_tools}

新增的工具：{added_tools}

生成内容要求：
1. **使用Markdown标题**：用 ## 开头
2. **第一句回顾**：简要总结之前的执行情况（20字以内，如"基于前X步执行情况"）
3. **第二句说明**：说明工具已重新筛选，强调"基于之前成果"、"继续优化"等延续性词汇
4. **第三句详情**：简要说明工具的变化（保留/替换/新增的数量）
5. **语气**：自然、专业、体现连贯性
6. **总字数**：50-80字
7. **格式**：第一行是 ## 标题，空行后接正文

示例1（工具有变化）：
## 🔄 工具配置已优化

基于前3步执行情况，已重新筛选工具配置：保留2个成功工具，替换1个失效工具，新增1个验证工具，当前共5个工具就位，准备继续执行。

示例2（工具数量增加）：
## ✅ 工具补充完成

根据之前的执行反馈，在原有3个工具基础上新增2个辅助工具，当前共5个工具已配齐，为后续步骤提供更全面支持。

示例3（工具精简）：
## 🎯 工具配置精简

基于执行历史分析，从之前8个工具中精选出5个核心工具，去除冗余、聚焦关键任务，配置更加高效。

示例4（工具数量不变但有调整）：
## 🔧 工具配置调整

继续之前的任务，保留3个成功工具，替换2个低效工具为更匹配的选项，当前5个工具已优化到位。

注意：
- 必须用 ## 开头作为Markdown标题
- 标题要体现具体动作（优化/补充/精简/调整），不要总用"完成"
- 标题后必须空行再接正文
- 必须体现"继续"、"基于"等延续性，而非"重新开始"
- 要具体说明工具数量变化（保留X个、替换Y个、新增Z个）
- 如果工具没有变化，说明"工具配置保持不变，继续执行"
"#;

/// 重新规划工具筛选完成消息模板
pub const REPLANNING_TOOL_SELECTION_COMPLETED_TEMPLATE: &str = r#"
任务：执行遇到问题后，系统已根据失败分析重新筛选工具。

失败原因摘要：{failure_summary}

之前使用的工具数量：{previous_tool_count}

当前筛选到的工具数量：{current_tool_count}

保留的成功工具：{kept_tools}

替换的失败工具：{replaced_tools}

新增的补充工具：{added_tools}

反思建议摘要：{reflection_suggestions}

生成内容要求：
1. **使用Markdown标题**：用 ## 开头
2. **第一句问题回顾**：简要说明遇到的问题（20字以内，中性表达）
3. **第二句优化说明**：说明已根据反思结果优化工具配置
4. **第三句变更详情**：具体说明保留/替换/新增的工具数量
5. **语气**：积极、解决导向、传达信心
6. **总字数**：60-90字
7. **格式**：第一行是 ## 标题，空行后接正文

示例1（替换失败工具）：
## 🔧 工具配置已优化

执行中发现「genesis_code_generate」工具无法连接，已根据反思分析重新配置：保留2个成功工具，替换失效工具为本地代码生成方案，新增1个验证工具，当前共4个工具就位。

示例2（增加补充工具）：
## ✅ 工具能力已增强

之前缺少数据格式验证环节导致问题，在原有5个工具基础上新增2个验证工具，补齐能力缺口，当前共7个工具已配齐。

示例3（精简无效工具）：
## 🎯 工具配置已精简

分析发现部分工具冗余导致执行混乱，从之前10个工具中精选出6个核心工具，去除重复、聚焦关键任务，配置更加高效。

示例4（全面替换）：
## 🔄 工具策略已调整

原有工具与任务需求不匹配，根据反思建议全面更换工具集：采用新的技术路线，当前5个工具已就位，避开之前的问题区域。

注意：
- 必须用 ## 开头作为Markdown标题
- 标题要体现优化动作（优化/增强/精简/调整），传达积极改进的信号
- 标题后必须空行再接正文
- 不要过多强调"失败"，重点放在"已解决"、"已优化"
- 必须具体说明工具变更（保留X个、替换Y个、新增Z个）
- 体现"根据反思"、"基于分析"等，说明优化是有依据的
"#;

/// 步骤完成进度报告模板
pub const STEP_COMPLETED_MESSAGE_TEMPLATE: &str = r#"
任务：一个步骤刚刚执行完成（可能成功或失败），需要根据实际执行结果报告进度。

整体任务：{task_description}

已完成步骤：{completed_step_name}

步骤输出：{step_output}

当前进度：{step_index}/{total_steps}

下一步骤：{next_step_name}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【场景指导】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{scene_specific_guidance}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ **核心要求：用Markdown标题 + 列表，丰富且多样化呈现**

【输出结构】
第一行：## 主标题（emoji + 状态 + 步骤名 + 进度）
后续每段：## 章节标题 + 列表/表格

⚠️ **重要变化要求**：
1. **章节标题每次都要变化**，不要总用"核心成果"、"详细内容"
2. **格式要智能选择**：
   - 数据型内容（键值对、统计、参数）→ 用表格
   - 描述型内容（成果、步骤、方案）→ 用列表
   - 同一输出中表格和列表混合使用
3. **信息组织要合理**，相关的信息可以放在同一章节

✅ **智能混合示例1**（数据用表格，步骤用列表）：
## ✅ BMS数据采集完成(2/5)

## 采集数据

| 指标 | 数值 |
|------|------|
| 采集点数 | 456个 |
| 覆盖范围 | 全楼18层 |
| 数据完整性 | 98% |
| 采集耗时 | 2.3秒 |
| 文件大小 | 3.2MB |

---

## 下一步
- 即将执行「数据清洗与处理」步骤

✅ **智能混合示例2**（参数用表格，成果用列表）：
## ✅ 建筑几何建模完成(1/4)

## 模型参数

| 项目 | 详情 |
|------|------|
| 建筑类型 | 18层商业写字楼 |
| 房间总数 | 1,250个 |
| 模型精度 | `LOD300` (高精度) |
| 建筑面积 | 45,000 m² |

---

## 生成成果
- 成功创建了完整的建筑几何模型
- 所有房间边界清晰，空间关系准确
- 模型文件: [building_001.ifc](/models/building_001.ifc) - IFC格式，156MB

✅ **智能混合示例3**（配置用列表）：
## ✅ 配置文件生成完成(3/3)

## 生成内容
- 配置文件:「system_config.json」
- 包含参数: 23个配置项
- 验证状态: ✅ 通过
- 任务全部完成 ✨

❌ **失败示例**（错误用表格，方案用列表）：
## ❌ 设备拓扑构建失败(3/6)

## 错误详情

| 字段 | 内容 |
|------|------|
| 错误类型 | 函数未定义 |
| 函数名称 | `create_chiller_connections` |
| 影响范围 | 冷机系统拓扑无法建立 |

---

## 处理方案
- 系统正在分析函数缺失原因
- 准备使用备用连接方法重试
- 预计5分钟内完成修复

【关键要求】
1. **主标题**：用 ## 开头，emoji + 简短状态 + 进度
2. **章节标题**：用 ## 开头，**每次都要变化**，从下列选项灵活选择：
   - 数据类: 采集概况、数据统计、技术指标、关键数据
   - 建模类: 模型信息、几何数据、建筑信息、设备详情
   - 文件类: 输出文件、生成内容、文件详情、报表信息
   - 执行类: 执行结果、完成情况、操作详情、处理结果
   - 状态类: 当前进度、任务状态、执行状态
   - 后续类: 即将进行、下一步、后续步骤
   - 错误类: 错误信息、问题描述、异常详情、处理方案
3. **章节数量**：信息丰富时2-4个，信息简单时1-2个
4. **内容呈现 - 智能混合表格和列表**：

   **决策规则**：
   - **数据型章节** → 用表格（如：采集数据、模型参数、文件信息、统计指标）
   - **描述型章节** → 用列表（如：生成成果、下一步、处理方案、关键亮点）
   - **同一输出必须混合使用**：不能全是表格，也不能全是列表

   **表格格式**：
   ```
   | 字段 | 值 |
   |------|------|
   | 项目1 | 数据1 |
   | 项目2 | 数据2 |
   ```

   **列表格式**：
   - 普通列表: `- 内容描述`
   - 强调列表: `- **关键字段**: 值`
   - 带链接: `- [文件名](路径)`

   **分割线**: 章节之间用 `---` 分隔

5. **保留字段**：report_id、title、summary、highlights、file_url、generated_time等都要展示
6. **真实准确**：只展示步骤输出中存在的内容，不要编造
7. **格式多样化**：每次输出都要表格和列表混合，不要单一格式
8. **禁止省略数据**：
   - 所有UUID、ID必须完整显示，禁止使用 `...` 省略
   - 例如：`2d4eff6e...` 是错误的，必须写完整 `2d4eff6e-f4ca-4dc6-b776-1d838b8cefb5`
   - 路径、URL、时间戳等必须完整展示
"#;

/// 评估结果消息模板
pub const EVALUATION_RESULT_MESSAGE_TEMPLATE: &str = r#"
任务：刚刚完成了一个步骤的评估。

步骤名称：{step_name}

步骤输出：{step_output}

评分：{score}/100

是否成功：{is_successful}

当前进度：{step_index}/{total_steps}

下一步骤：{next_step_name}

成功要点：{successes}

失败原因：{failures}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
【场景指导】
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{scene_specific_guidance}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

生成内容要求（必须严格遵守）：

**必须使用以下格式：**

如果有下一步（{next_step_name} 不是"无"）：
「步骤名称」评估得分 X/100，[执行结果的关键数据]。接下来将进行「下一步骤名称」。

如果是最后一步（{next_step_name} 是"无"）：
「步骤名称」评估得分 X/100，[执行结果的关键数据]。

**具体要求：**
1. 必须使用「」包裹步骤名称，使用{step_name}的原文
2. 必须写明"评估得分 {score}/100"
3. 必须从{successes}中提取关键数据，避免"完美"、"优秀"等主观词
4. 只有当{next_step_name}不是"无"时，才添加"接下来将进行「{next_step_name}」"
5. 如果{next_step_name}是"无"，说明这是最后一步，以句号结尾即可
6. 总字数控制在60字以内

**标准格式示例（必须参考）：**
「建筑几何建模」评估得分 92/100，生成18层建筑3D模型，包含1250个房间。接下来将进行「设备数字孪生建模」。

「BMS数据采集」评估得分 88/100，采集456个数据点，覆盖率98%。接下来将进行「节能诊断」。

「系统拓扑构建」评估得分 95/100，连接125个设备节点，建立340条关系。接下来将进行「BMS数据采集」。

「节能诊断与优化」评估得分 100/100，完成能耗分析，识别出15个优化点。

**注意：**
- 禁止使用"完美"、"优秀"、"很好"等主观评价词
- 必须用具体数据和事实说话
- 步骤名称和下一步名称都必须用「」包裹
- 必须包含"评估得分 X/100"字样
- 最后一步不要说"接下来将进行"
"#;

/// 任务完成消息模板
pub const TASK_COMPLETION_MESSAGE_TEMPLATE: &str = r#"
任务：用户的任务已经全部完成，需要生成祝贺消息。

任务描述：{task_description}

完成的步骤：{completed_steps}

总体评分：{overall_score}/100

【生成要求：用Markdown标题 + 列表的庆祝消息，格式要多样化】

**输出结构：**
## 主标题（emoji + 任务完成状态）
## 章节标题 + Markdown列表/表格

⚠️ **多样化要求**: 每次完成消息的章节标题、emoji和格式都要有变化！

**多样化示例1**（表格型 - 推荐）：
## 🎉 数据采集任务完成

## 任务总结

| 项目 | 数值 |
|------|------|
| 完成步骤 | 3/3 |
| 总体评分 | 92/100 |
| 数据完整性 | 98% |

**多样化示例2**（混合型 - 表格+列表）：
## 🎊 数字孪生建模任务完成

## 执行概况

| 指标 | 结果 |
|------|------|
| 总步骤数 | 6个步骤 |
| 完成状态 | 全部成功 ✨ |
| 综合评分 | 95/100 |

---

## 核心成果
- 建筑模型: 18层写字楼，1,250个房间
- 设备节点: 125个设备，340条连接关系
- 数据采集: 456个数据点，覆盖率98%

---

## 输出文件
- [模型文件](/output/model.ifc) - IFC格式，156MB
- [数据文件](/output/data.json) - JSON格式，3.2MB
- [报表文件](/output/report.pdf) - PDF格式，2.1MB

**多样化示例3**（列表型）：
## ✨ 智能诊断任务完成

## 完成情况
- **执行步骤**: 4/4 全部完成
- **任务评分**: 98分 (优秀)

---

## 诊断成果
- 识别问题: 15个优化点
- 节能潜力: 预计降低能耗12%
- 建议方案: 生成3套优化方案

**要求：**
1. **主标题emoji变化**: 🎉 / 🎊 / ✨ / 🎯 / 🏆 等，不要总用同一个
2. **章节标题要变化**: 执行概况、任务总结、核心成果、完成情况、诊断成果、输出文件等
3. **格式要智能混合**:
   - 统计数据、评分 → 用表格
   - 成果描述、文件列表 → 用列表
   - 必须混合使用，不要单一格式
4. **章节数量**：信息简单时1-2个，信息丰富时2-3个
5. **禁止省略数据**：
   - UUID、ID、路径等必须完整显示，禁止用 `...` 省略
   - 错误示例：`2d4eff6e...`
   - 正确示例：`2d4eff6e-f4ca-4dc6-b776-1d838b8cefb5`
"#;

/// 消息润色系统提示词 - 用于将技术性内容转换为用户友好的表达
pub const MESSAGE_POLISH_SYSTEM_PROMPT: &str = "你是一个专业的文本润色助手。你的任务是将技术性的输出内容转换为更人性化、更流畅的表达，同时保持信息的准确性。";

/// 消息润色用户提示词模板
pub const MESSAGE_POLISH_USER_TEMPLATE: &str = r#"请将以下{message_type}内容润色为更人性化、更流畅的表达：

原始内容：
{raw_message}

润色要求：
1. 保持所有关键信息和数据的准确性
2. 使用更自然、友好的语言
3. 添加适当的过渡词，让内容更流畅
4. 保持简洁，不要过度冗长，控制在2-3句话以内
5. 可以使用适当的 emoji 增强表现力（但不要过度，最多2-3个）
6. 直接输出润色后的内容，不要有其他解释
7. 禁止使用以下词汇和表达方式：
   - "任务正在排队中"、"排队"、"请稍等片刻"等等待类词汇
   - "当然！以下是"、"好的！这是"等客套开场白
   - "润色"、"优化"等元语言词汇
   - 使用更直接、更自然的表达，例如：
     * "正在准备..." 而不是 "任务正在排队中"
     * "开始分析..." 而不是 "当然！以下是分析结果"
     * 直接说明正在做什么，而不是描述过程本身
8. 输出格式：紧凑简洁，避免使用过多换行符，多个短句用顿号或逗号连接而不是换行

润色后的内容：
"#;

/// 消息模板构建器
pub struct MessageTemplateBuilder {
    /// 统一的场景管理器
    scene_manager: SceneManager,
}

impl Default for MessageTemplateBuilder {
    fn default() -> Self {
        Self::new()
    }
}

impl MessageTemplateBuilder {
    /// 创建新的消息模板构建器
    pub fn new() -> Self {
        Self {
            scene_manager: SceneManager::new(),
        }
    }
    /// 构建输出风格转换提示词
    pub fn build_output_style_prompt(content: &str, max_length: usize) -> String {
        OUTPUT_STYLE_TEMPLATE
            .replace("{content}", content)
            .replace("{max_length}", &max_length.to_string())
    }

    /// 构建工具筛选阶段消息提示词
    ///
    /// # 参数
    /// - `task_description`: 任务描述
    /// - `tool_count`: 工具数量
    /// - `task_type`: 任务类型（用于场景匹配）
    pub fn build_tool_selection_message(
        &self,
        task_description: &str,
        tool_count: usize,
        task_type: Option<&str>,
    ) -> String {
        let scene_guidance = self.scene_manager.get_message_guidance(task_type);

        TOOL_SELECTION_MESSAGE_TEMPLATE
            .replace("{task_description}", task_description)
            .replace("{tool_count}", &tool_count.to_string())
            .replace("{scene_specific_guidance}", scene_guidance)
    }

    /// 构建任务规划阶段消息提示词
    pub fn build_task_planning_message(
        task_description: &str,
        tool_count: usize,
        step_count: usize,
    ) -> String {
        TASK_PLANNING_MESSAGE_TEMPLATE
            .replace("{task_description}", task_description)
            .replace("{tool_count}", &tool_count.to_string())
            .replace("{step_count}", &step_count.to_string())
    }

    /// 构建重新规划阶段消息提示词
    pub fn build_replanning_message(
        execution_summary: &str,
        issue: &str,
        completed_steps_count: usize,
        failed_step_name: &str,
    ) -> String {
        REPLANNING_MESSAGE_TEMPLATE
            .replace("{execution_summary}", execution_summary)
            .replace("{issue}", issue)
            .replace("{completed_steps_count}", &completed_steps_count.to_string())
            .replace("{failed_step_name}", failed_step_name)
    }

    /// 构建继续规划工具筛选完成消息提示词
    pub fn build_continue_planning_tool_selection_completed_message(
        execution_summary: &str,
        previous_tool_count: usize,
        current_tool_count: usize,
        kept_tools: &str,
        replaced_tools: &str,
        added_tools: &str,
    ) -> String {
        let change_type = if current_tool_count > previous_tool_count {
            "新增"
        } else if current_tool_count < previous_tool_count {
            "优化精简"
        } else {
            "调整"
        };

        CONTINUE_PLANNING_TOOL_SELECTION_COMPLETED_TEMPLATE
            .replace("{execution_summary}", execution_summary)
            .replace("{previous_tool_count}", &previous_tool_count.to_string())
            .replace("{current_tool_count}", &current_tool_count.to_string())
            .replace("{change_type}", change_type)
            .replace("{kept_tools}", kept_tools)
            .replace("{replaced_tools}", replaced_tools)
            .replace("{added_tools}", added_tools)
    }

    /// 构建重新规划工具筛选完成消息提示词
    pub fn build_replanning_tool_selection_completed_message(
        failure_summary: &str,
        previous_tool_count: usize,
        current_tool_count: usize,
        kept_tools: &str,
        replaced_tools: &str,
        added_tools: &str,
        reflection_suggestions: &str,
    ) -> String {
        REPLANNING_TOOL_SELECTION_COMPLETED_TEMPLATE
            .replace("{failure_summary}", failure_summary)
            .replace("{previous_tool_count}", &previous_tool_count.to_string())
            .replace("{current_tool_count}", &current_tool_count.to_string())
            .replace("{kept_tools}", kept_tools)
            .replace("{replaced_tools}", replaced_tools)
            .replace("{added_tools}", added_tools)
            .replace("{reflection_suggestions}", reflection_suggestions)
    }

    /// 构建步骤完成进度报告提示词
    ///
    /// # 参数
    /// - `task_description`: 任务描述
    /// - `completed_step_name`: 已完成的步骤名称
    /// - `step_output`: 步骤输出
    /// - `step_index`: 当前步骤索引
    /// - `total_steps`: 总步骤数
    /// - `next_step_name`: 下一步骤名称
    /// - `task_type`: 任务类型（用于场景匹配）
    pub fn build_step_completed_message(
        &self,
        task_description: &str,
        completed_step_name: &str,
        step_output: &str,
        step_index: usize,
        total_steps: usize,
        next_step_name: &str,
        task_type: Option<&str>,
    ) -> String {
        let scene_guidance = self.scene_manager.get_message_guidance(task_type);

        STEP_COMPLETED_MESSAGE_TEMPLATE
            .replace("{task_description}", task_description)
            .replace("{completed_step_name}", completed_step_name)
            .replace("{step_output}", step_output)
            .replace("{step_index}", &step_index.to_string())
            .replace("{total_steps}", &total_steps.to_string())
            .replace("{next_step_name}", next_step_name)
            .replace("{scene_specific_guidance}", scene_guidance)
    }

    /// 构建评估结果消息提示词
    ///
    /// # 参数
    /// - `step_name`: 步骤名称
    /// - `step_output`: 步骤输出
    /// - `score`: 评分
    /// - `is_successful`: 是否成功
    /// - `step_index`: 当前步骤索引
    /// - `total_steps`: 总步骤数
    /// - `next_step_name`: 下一步骤名称
    /// - `successes`: 成功要点
    /// - `failures`: 失败原因
    /// - `task_type`: 任务类型（用于场景匹配）
    pub fn build_evaluation_result_message(
        &self,
        step_name: &str,
        step_output: &str,
        score: u32,
        is_successful: bool,
        step_index: usize,
        total_steps: usize,
        next_step_name: &str,
        successes: &str,
        failures: &str,
        task_type: Option<&str>,
    ) -> String {
        let scene_guidance = self.scene_manager.get_message_guidance(task_type);

        EVALUATION_RESULT_MESSAGE_TEMPLATE
            .replace("{step_name}", step_name)
            .replace("{step_output}", step_output)
            .replace("{score}", &score.to_string())
            .replace("{is_successful}", &is_successful.to_string())
            .replace("{step_index}", &step_index.to_string())
            .replace("{total_steps}", &total_steps.to_string())
            .replace("{next_step_name}", next_step_name)
            .replace("{successes}", successes)
            .replace("{failures}", failures)
            .replace("{scene_specific_guidance}", scene_guidance)
    }

    /// 构建任务完成消息提示词
    pub fn build_task_completion_message(
        task_description: &str,
        completed_steps: &str,
        overall_score: u32,
    ) -> String {
        TASK_COMPLETION_MESSAGE_TEMPLATE
            .replace("{task_description}", task_description)
            .replace("{completed_steps}", completed_steps)
            .replace("{overall_score}", &overall_score.to_string())
    }

    /// 构建消息润色提示词
    pub fn build_message_polish_prompt(
        message_type: &str,
        raw_message: &str,
    ) -> (String, String) {
        let user_prompt = MESSAGE_POLISH_USER_TEMPLATE
            .replace("{message_type}", message_type)
            .replace("{raw_message}", raw_message);

        (MESSAGE_POLISH_SYSTEM_PROMPT.to_string(), user_prompt)
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_build_output_style_prompt() {
        let prompt = MessageTemplateBuilder::build_output_style_prompt("测试内容", 50);
        assert!(prompt.contains("测试内容"));
        assert!(prompt.contains("50"));
    }

    #[test]
    fn test_build_tool_selection_message() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_tool_selection_message("创建模型", 10, None);
        assert!(message.contains("创建模型"));
        assert!(message.contains("10"));
        assert!(message.contains("场景指导"));
    }

    #[test]
    fn test_build_task_planning_message() {
        let message = MessageTemplateBuilder::build_task_planning_message("测试任务", 5, 3);
        assert!(message.contains("测试任务"));
        assert!(message.contains("5"));
        assert!(message.contains("3"));
    }

    #[test]
    fn test_build_replanning_message() {
        let message = MessageTemplateBuilder::build_replanning_message("工具未找到");
        assert!(message.contains("工具未找到"));
    }

    #[test]
    fn test_message_template_builder_new() {
        let _builder = MessageTemplateBuilder::new();
        // 只验证可以成功创建
    }

    #[test]
    fn test_build_step_completed_message() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_step_completed_message(
            "整体任务",
            "步骤1",
            "成功",
            1,
            3,
            "步骤2",
            None,
        );
        assert!(message.contains("整体任务"));
        assert!(message.contains("步骤1"));
        assert!(message.contains("成功"));
        assert!(message.contains("1"));
        assert!(message.contains("3"));
        assert!(message.contains("步骤2"));
        assert!(message.contains("场景指导"));
    }

    #[test]
    fn test_build_evaluation_result_message() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_evaluation_result_message(
            "测试步骤",
            "输出结果",
            95,
            true,
            2,
            5,
            "下一步",
            "成功要点",
            "失败原因",
            None,
        );
        assert!(message.contains("测试步骤"));
        assert!(message.contains("95"));
        assert!(message.contains("true"));
        assert!(message.contains("场景指导"));
    }

    #[test]
    fn test_build_task_completion_message() {
        let message =
            MessageTemplateBuilder::build_task_completion_message("完成的任务", "步骤列表", 90);
        assert!(message.contains("完成的任务"));
        assert!(message.contains("步骤列表"));
        assert!(message.contains("90"));
    }

    #[test]
    fn test_build_message_polish_prompt() {
        let (system, user) =
            MessageTemplateBuilder::build_message_polish_prompt("进度报告", "原始消息");
        assert!(system.contains("润色助手"));
        assert!(user.contains("进度报告"));
        assert!(user.contains("原始消息"));
    }

    #[test]
    fn test_message_templates_constants() {
        assert!(!OUTPUT_STYLE_TEMPLATE.is_empty());
        assert!(!TOOL_SELECTION_MESSAGE_TEMPLATE.is_empty());
        assert!(!TASK_PLANNING_MESSAGE_TEMPLATE.is_empty());
        assert!(!REPLANNING_MESSAGE_TEMPLATE.is_empty());
        assert!(!STEP_COMPLETED_MESSAGE_TEMPLATE.is_empty());
        assert!(!EVALUATION_RESULT_MESSAGE_TEMPLATE.is_empty());
        assert!(!TASK_COMPLETION_MESSAGE_TEMPLATE.is_empty());
        assert!(!MESSAGE_POLISH_SYSTEM_PROMPT.is_empty());
        assert!(!MESSAGE_POLISH_USER_TEMPLATE.is_empty());
    }

    #[test]
    fn test_tool_selection_message_with_scene_guidance() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_tool_selection_message(
            "创建工具",
            15,
            Some("工具管理"),
        );
        assert!(message.contains("创建工具"));
        assert!(message.contains("15"));
        assert!(message.contains("场景指导"));
        // 验证包含工具管理场景的特定指导
        assert!(message.contains("工具管理") || message.contains("场景"));
    }

    #[test]
    fn test_step_completed_message_with_scene_guidance() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_step_completed_message(
            "数字孪生建模",
            "BMS数据采集",
            "{\"success\": true, \"msg\": \"采集成功\"}",
            1,
            5,
            "几何建模",
            Some("自然语言建模"),
        );
        assert!(message.contains("数字孪生建模"));
        assert!(message.contains("BMS数据采集"));
        assert!(message.contains("场景指导"));
    }

    #[test]
    fn test_evaluation_result_message_with_scene_guidance() {
        let builder = MessageTemplateBuilder::new();
        let message = builder.build_evaluation_result_message(
            "数据采集",
            "采集完成",
            92,
            true,
            1,
            3,
            "数据分析",
            "成功采集数据",
            "无",
            Some("数据分析"),
        );
        assert!(message.contains("数据采集"));
        assert!(message.contains("92"));
        assert!(message.contains("场景指导"));
    }
}
